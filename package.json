{
  "name": "sabia-claude-plugin",
  "version": "0.3.0-beta.1",
  "private": true,
  "type": "module",
  "license": "MIT",
  "packageManager": "pnpm@11.9.0",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "test": "vitest run",
    "check": "node --check scripts/sabia.mjs && node --check scripts/sabia-http.mjs && node --check scripts/sabia-report-binding.mjs && node --check cowork/scripts/sabia.mjs && node --check cowork/scripts/sabia-http.mjs"
  },
  "devDependencies": {
    "vitest": "4.1.10"
  }
}
